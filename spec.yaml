id: gcf_event_download
requirements:
  - name: ecoscope-platform
    version: ">=2.11.14, <2.12.0"
    channel: https://repo.prefix.dev/ecoscope-workflows/
  - name: ecoscope-workflows-ext-custom
    version: "0.1.0rc6"
    channel: https://repo.prefix.dev/ecoscope-workflows-custom/

rjsf-overrides:
  properties:
    get_event_data.properties.event_types.description: >-
      Specify the event type(s) to download (optional). Enter the VALUE (not
      display name) for each event type — one per field (e.g.
      "giraffe_survey_encounter_nam"). Values can be found in your EarthRanger
      Admin site under Activity → Event Types. Leave empty to include all event
      types.
    get_event_data.properties.event_types.title: "Event Types"

    persist_events.properties.filename_prefix.default: "events"
    persist_events.properties.filetypes.default: ["csv"]
    persist_events.properties.filetypes.items.enum: ["csv", "parquet"]
    persist_events.properties.filetypes.description: >-
      Output file format(s). CSV opens directly in Excel. Parquet preserves
      geometry natively and is recommended for spatial analysis.

    Process Events.properties.process_columns.properties.drop_columns.default:
      - level_8
      - index
      - location
      - reported_by
      - event_details
      - geojson
      - attributes
      - notes
      - patrols
      - patrol_segments
      - is_contained_in
      - related_subjects
      - location_lat
      - location_lon
      - message
      - provenance
      - event_category
      - priority_label
      - comment
      - end_time
      - sort_at
      - icon_id
      - url
      - image_url
      - external_source
    Process Events.properties.process_columns.properties.drop_columns.description: >-
      Columns to remove from the final export. The defaults match the GCF
      Twiga Tools Event Download page. Adjust as needed for your use case.

    Process Events.properties.groupers.properties.groupers.items.anyOf:
      - {"$ref": "#/$defs/ValueGrouper"}
      - {"$ref": "#/$defs/TemporalGrouper"}

  $defs:
    ValueGrouper:
      title: "Category"
      type: "object"
      properties:
        index_name:
          title: "Index Name"
          type: "string"
          oneOf:
            - const: "event_category"
              title: "Event Type Category"
            - const: "event_type"
              title: "Event Type"
            - const: "reported_by_name"
              title: "Reported By Name"

  uiSchema:
    Process Events.filter_events.filter_point_coords.items.ui:options.label: false

task-instance-defaults:
  skipif:
    conditions:
      - any_is_empty_df
      - any_dependency_skipped

workflow:

  - name: Workflow Details
    id: workflow_details
    task: set_workflow_details

  - name: Time Range
    id: time_range
    task: set_time_range
    partial:
      time_format: "%d %b %Y %H:%M:%S"

  - name: Extract Timezone Selection
    id: get_timezone
    task: get_timezone_from_time_range
    partial:
      time_range: ${{ workflow.time_range.return }}

  - name: Data Source
    id: er_client_name
    task: set_er_connection

  - name: Get Event Data
    id: get_event_data
    task: get_events
    partial:
      client: ${{ workflow.er_client_name.return }}
      time_range: ${{ workflow.time_range.return }}
      event_columns: null
      raise_on_empty: false
      include_details: true
      include_updates: false
      include_related_events: false
      include_display_values: true

  - title: Process Events
    type: task-group
    description: >-
      Flatten and clean event data: timezone conversion, event detail
      normalisation, GCF repeat-group handling, column filtering, and optional
      SQL transformation.
    tasks:

      - name: Convert to timezone
        id: convert_to_user_timezone
        task: convert_values_to_timezone
        partial:
          df: ${{ workflow.get_event_data.return }}
          timezone: ${{ workflow.get_timezone.return }}
          columns: ["time"]
          auto_detect: false

      - name: Extract reported_by_name from Events
        id: extract_reported_by
        task: extract_value_from_json_column
        partial:
          df: ${{ workflow.convert_to_user_timezone.return }}
          column_name: "reported_by"
          field_name_options: ["name"]
          output_type: "str"
          output_column_name: "reported_by_name"

      - name: Extract reported_by_subtype from Events
        id: extract_reported_by_subtype
        task: extract_value_from_json_column
        partial:
          df: ${{ workflow.extract_reported_by.return }}
          column_name: "reported_by"
          field_name_options: ["subject_subtype"]
          output_type: "str"
          output_column_name: "reported_by_subtype"

      - name: Process Event Details
        id: process_event_details
        task: process_events_details
        partial:
          df: ${{ workflow.extract_reported_by_subtype.return }}
          client: ${{ workflow.er_client_name.return }}
          map_to_titles: true
          ordered: true

      - name: Normalize Event Details
        id: normalize_event_details
        task: normalize_json_column
        partial:
          df: ${{ workflow.process_event_details.return }}
          column: "event_details"
          skip_if_not_exists: true
          sort_columns: false

      - name: Remove Column Prefix from Event Details
        id: drop_event_details_prefix
        task: drop_column_prefix
        partial:
          df: ${{ workflow.normalize_event_details.return }}
          prefix: "event_details__"
          duplicate_strategy: "suffix"

      - name: Convert event detail datetimes to timezone
        id: convert_event_details_timezone
        task: convert_values_to_timezone
        partial:
          df: ${{ workflow.drop_event_details_prefix.return }}
          timezone: ${{ workflow.get_timezone.return }}
          columns: []
          auto_detect: true

      # GCF CUSTOM TASK — repeat-group flattening
      # See custom_tasks/flatten_repeat_groups.py for the implementation.
      # Uncomment once packaged in ecoscope-workflows-ext-gcf:
      #
      # - name: Flatten GCF Repeat Groups
      #   id: flatten_repeat_groups
      #   task: flatten_gcf_repeat_groups
      #   partial:
      #     df: ${{ workflow.convert_event_details_timezone.return }}
      #
      # Until then: events without repeat groups export correctly.
      # Repeat-group events will have one row per event instead of one
      # row per repeat-group entry.

      - name: Filter Event Relocations
        id: filter_events
        task: apply_reloc_coord_filter
        partial:
          df: ${{ workflow.convert_event_details_timezone.return }}
          roi_gdf: null
          roi_name: null
          reset_index: true

      - name: Preprocess Columns
        id: process_columns
        task: map_columns
        partial:
          df: ${{ workflow.filter_events.return }}
          rename_columns:
            time: event_datetime
            id: event_id
          retain_columns: []
          raise_if_not_found: false

      - name: Apply SQL Query
        id: sql_query
        task: apply_sql_query
        partial:
          df: ${{ workflow.process_columns.return }}

      - name: Group Data
        id: groupers
        task: set_groupers

      - name: Add temporal index to Events
        id: events_add_temporal_index
        task: add_temporal_index
        partial:
          df: ${{ workflow.sql_query.return }}
          time_col: "event_datetime"
          groupers: ${{ workflow.groupers.return }}
          cast_to_datetime: true
          format: "mixed"

      - name: Split Events by Group
        id: split_event_groups
        task: split_groups
        partial:
          df: ${{ workflow.events_add_temporal_index.return }}
          groupers: ${{ workflow.groupers.return }}

  - name: Persist Events
    id: persist_events
    task: persist_df_wrapper
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      sanitize: true
    mapvalues:
      argnames: df
      argvalues: ${{ workflow.split_event_groups.return }}
    skipif:
      conditions:
        - never

  - name: Create Dashboard
    id: events_dashboard
    task: gather_dashboard
    partial:
      details: ${{ workflow.workflow_details.return }}
      widgets: []
      groupers: ${{ workflow.groupers.return }}
      time_range: ${{ workflow.time_range.return }}
